# Keep this repo focused on the AI app under /ai
*
!.gitignore
!.gitattributes
!README.md
!ai/
!ai/**

# Extra safety for secrets/runtime state inside app
ai/config.php
ai/.installed

# Admin password protection files (local only, never commit)
ai/admin/.htaccess
ai/admin/.htpasswd
